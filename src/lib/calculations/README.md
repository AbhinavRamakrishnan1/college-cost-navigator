# Calculation boundary

Phase 1 intentionally contains no federal-aid, net-price, or repayment calculations.

Future arithmetic must use `decimal.js`, the frozen `docs/calculation-contract-v1.0.md`, and exact-version policy selectors. Unsupported or unavailable policy inputs must fail closed.
