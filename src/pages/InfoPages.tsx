import { PageShell, PlaceholderCard } from '../components/PageShell'

export function HowItWorksPage() { return <PageShell eyebrow="How it works" title="One planning journey, in understandable steps." intro="Build a local household profile, explore federal school data, and estimate Direct Loan repayment with explicit assumptions."><div className="grid gap-4 md:grid-cols-3">{['Describe your household','Explore schools and aid','Consider repayment'].map((title, i) => <div className="card p-6" key={title}><span className="eyebrow">Step {i + 1}</span><h2 className="mt-3 text-xl font-bold">{title}</h2><p className="mt-2 text-sm leading-6 text-ink-700">Use the navigator to explore this step. Estimates are not financial-aid offers or guaranteed repayment schedules.</p></div>)}</div></PageShell> }
export function PrivacyPage() { return <PageShell eyebrow="Privacy" title="Your financial life should not become a data trail." intro="There is no account or analytics. A supported household profile can be stored locally in this browser and is never included in network requests."><div className="grid gap-4 sm:grid-cols-2"><PlaceholderCard title="Local by design"><p>Profile data uses this browser's IndexedDB storage. It stays on this device unless you clear it.</p></PlaceholderCard><PlaceholderCard title="You stay in control"><p>Delete all locally stored navigator data at any time from Settings. Versioned JSON backups can be exported and restored locally in Settings.</p></PlaceholderCard></div></PageShell> }
