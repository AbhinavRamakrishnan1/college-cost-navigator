import { PageShell, PlaceholderCard } from '../components/PageShell'

export function HowItWorksPage() { return <PageShell eyebrow="How it works" title="One planning journey, in understandable steps." intro="Phase 1 establishes the experience and safeguards. Calculations and school data arrive only after their policy rules and tests are ready."><div className="grid gap-4 md:grid-cols-3">{['Describe your household','Explore schools and aid','Consider repayment'].map((title, i) => <div className="card p-6" key={title}><span className="eyebrow">Step {i + 1}</span><h2 className="mt-3 text-xl font-bold">{title}</h2><p className="mt-2 text-sm leading-6 text-ink-700">This step is a guided placeholder in Phase 1.</p></div>)}</div></PageShell> }
export function MethodologyPage() { return <PageShell eyebrow="Methodology" title="Estimates should be explainable and versioned." intro="The authoritative calculation contract is v1.0.0. Future calculations will require an exact matching award year, cohort, and guideline year."><PlaceholderCard title="Phase 1 boundary"><p>No SAI, Pell, net price, RAP, IBR, or Tiered Standard arithmetic is implemented. Independent-student SAI formulas are unsupported in v1.</p></PlaceholderCard></PageShell> }
export function PolicyChangesPage() { return <PageShell eyebrow="Policy changes" title="Federal rules change. Your estimate should say when they do." intro="This page will explain version changes that affect estimates. Phase 1 recognizes the frozen v1.0.0 contract and refuses unavailable versions."><PlaceholderCard title="Current implementation status"><p>Policy contract v1.0.0 · verified August 7, 2026 · implementation guarded to the declared scope.</p></PlaceholderCard></PageShell> }
export function PrivacyPage() { return <PageShell eyebrow="Privacy" title="Your financial life should not become a data trail." intro="Phase 1 has no account, analytics, financial-input network requests, or local database. The fictional household is safe sample content—not your information."><div className="grid gap-4 sm:grid-cols-2"><PlaceholderCard title="What happens now"><p>Nothing you enter is persisted. There are no live financial input forms in this phase.</p></PlaceholderCard><PlaceholderCard title="Before persistence arrives"><p>Backup, restore, and delete-all behavior will be designed and tested before local storage is introduced.</p></PlaceholderCard></div></PageShell> }
